package com.o2o.until;

//获取历经的工具类
public class PathUtil {
    public static String getImage(){
        //获取操作系统的文件分割符
        String separator = System.getProperty("file.separator");
        String os = System.getProperty("os.name");
        String basepath = "";
        if (os.toLowerCase().equals("win")) {
            String a = "";
        }
        return "niaho";

    }
}
